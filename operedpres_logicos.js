// Operador lógico and && (afirmação) esse operador só fala que é true, se a "const" for igual se for diferente ele retornara "false"!
const freteGratis = true;
const estoque = false;

//console.log(freteGratis && estoque);

// Operador lógico ou || esse operador só fala que é true, se a "const" tiver pelo menos um true, caso contrario ela devolve false !!

//console.log(freteGratis || estoque);

// Operador lógico not (negação !) = Ele inverte o valor de verdadeiro para falso e vice-versa.
 
//console.log(!freteGratis);
//console.log(!estoque);

// dupla negação ||  Usada para converter um valor para seu equivalente booleano
const nome = null;

console.log(!!nome)




