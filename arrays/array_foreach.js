

// Incluides verifica se existe o item no arraye e devolve verdadiro ou falso

const alunos = ["Mathues", "Lucas", "Ana", "fulano"]; 

alunos.forEach((nome) => {
  console.log(`Bem vindos(a) ${nome}`);
});

/* sempre que for percorrer uma lista usar o
Array.forEach((array) =>{

})
  */