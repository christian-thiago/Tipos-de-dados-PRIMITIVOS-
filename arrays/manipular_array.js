const numeros = [1, 5, 10, 20];


/*
push > adiciona um item ou mais no ultimo elelmento do array
pop > remove o ultimo elementos no inicio do array
unshift > Adicionar elementos no inicio do array
shift > remover o primeiro elemento do array.
*/

numeros.push(30, 50, 100)

//numeros.pop();

// numeros.unshift(500)

numeros.shift();

console.log(numeros);

